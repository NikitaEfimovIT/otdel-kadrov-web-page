# Caddy сам получит и продлит бесплатный сертификат Let's Encrypt.
# Требования: DNS A-запись api.ВАШ-ДОМЕН.ru → публичный IP VM,
# и открытые порты 80 и 443.

{
	email admin@ВАШ-ДОМЕН.ru
}

api.ВАШ-ДОМЕН.ru {
	reverse_proxy strapi:1337
}
